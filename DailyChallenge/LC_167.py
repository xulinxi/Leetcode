# 167. Two Sum II - Input array is sorted


class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        
#         # *** (1-indexed) means returning the indices when index starting with 1 NOT 0!!!
            
#         i, j = 0, len(numbers) - 1
        
#         while i < j:
#             if numbers[i] + numbers[j] == target:
#                 return [i+1, j+1]
            
#             if numbers[i] + numbers[j] > target:
#                 j -= 1
                
#             if numbers[i] + numbers[j] < target:
#                 i += 1     
                
        
